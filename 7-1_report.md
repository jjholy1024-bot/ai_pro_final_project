# 7-1. 데이터 탐색 및 시각화

## 데이터셋 기본 정보

| 항목 | 내용 |
|------|------|
| 총 이미지 수 | 27,000개 |
| 총 클래스 수 | 150개 |
| 클래스당 이미지 수 | 180개 (균등) |
| 이미지 해상도 | 128 × 128px (통일) |
| 평균 밝기 | 129.8 / 255 |

---

## 7-1-1. 클래스별 샘플 수 분포

![클래스별 샘플 수 분포](7-1_class_distribution.png)

모든 클래스가 180개의 이미지로 구성되어 있어 완전한 균형 데이터셋임을 확인. 모두 동일하게 180개이며, 클래스 불균형으로 인한 성능 편향은 발생하지 않음

---

## 7-1-2. 각 클래스의 샘플 이미지 시각화

![전체 클래스 샘플 이미지](sampleimg_all.png)

![클래스별 샘플 이미지 상세](sampleimg_12.png)

150개 전체 클래스의 대표 샘플 이미지를 시각화한 결과, 다음과 같은 특징이 관찰되었다.

- **색상 다양성**: 붉은 계열(볶음류, 찌개류), 흰색 계열(국탕류, 나물류), 갈색 계열(구이류, 조림류) 등 음식 카테고리별로 색감 특징이 뚜렷하게 나타난다.
- **형태 유사성**: 동태찌개/김치찌개처럼 같은 찌개류는 그릇 형태와 국물 색이 유사하고, 갈치구이/고등어구이처럼 같은 조리법의 생선류는 외형이 거의 동일하게 보인다.
- **클래스 내 다양성**: 동일 클래스 내에서도 담음새, 조리 환경, 촬영 각도에 따라 이미지 편차가 크게 나타난다. 예를 들어 삼계탕의 경우 뚝배기/그릇/솥 등 다양한 형태로 제공된다.

---

## 7-1-3. 기타 데이터 분석

### 이미지 해상도 분포

![이미지 해상도 분포](7-1_resolution_distribution.png)

모든 이미지가 128 × 128px 정사각형으로 완전히 통일되어 있음. 너비/높이 분포가 단일 값(128)에 집중된 수직 막대 형태로 나타나며, 가로세로 비율 역시 1.0으로 일정.

이는 데이터셋이 사전에 전처리된 것으로, 별도의 리사이즈 전처리 없이 모델 입력으로 바로 사용 가능한 구조이다. 단, 128px는 세밀한 시각적 특징(예: 나물 종류, 조림 재료의 텍스처)을 구분하기에 낮은 해상도일 수 있어 Fine-grained 분류에 한계가 될 수 있음.

---

### 밝기 및 색상 분포

![밝기 및 색상 분포](7-1_brightness_color.png)

**밝기 분포**

이미지 밝기의 평균은 **129.8 / 255**로 중간값(127.5)에 근접, 분포는 50~225 범위에 걸쳐 완만한 정규분포 형태를 보임. 과도하게 어둡거나 밝은 극단적 이미지는 소수에 불과하여 전반적으로 양호한 밝기 분포를 가짐

**RGB 채널 분포**

- **B 채널(파랑)** 이 가장 낮은 픽셀값 분포를 보이며, 약 80~120 범위에 집중
- **G 채널(녹색)** 은 중간 범위에 분포
- **R 채널(빨강)** 이 가장 높은 픽셀값 분포를 보이며, 약 130~220 범위에 넓게 분포

R 채널이 상대적으로 높게 나타나는 것은 한국 음식의 특성(고추장, 김치, 볶음류 등 붉은 계열 음식 비중)을 반영한 결과로 해석됨. 데이터 정규화 시 채널별 평균과 표준편차를 별도로 적용하는 것이 효과적

---

### 중복 이미지 분석

전체 27,000개 이미지에 대해 MD5 해시 기반 중복 탐지를 수행한 결과는 다음과 같음

| 항목 | 수치 |
|------|------|
| 중복 그룹 수 | 318개 |
| 중복 이미지 수 | 319개 |
| 전체 대비 비율 | 1.18% |
| 클래스 내 중복 | 303그룹 |
| 클래스 간 중복 | 14그룹 |

**클래스 내 중복 (303그룹)** — 같은 클래스 내에서 동일한 이미지가 2개 존재하는 경우로 데이터 수집 과정에서 발생한 단순 중복. 모델 학습 시 동일 이미지를 반복 학습하게 되어 해당 샘플에 과적합될 수 있으므로 제거를 염두에 둘 것.

**클래스 간 중복 (14그룹)** — 서로 다른 클래스에 동일한 이미지가 존재하는 경우, 레이블 오류에 해당함

| 중복 클래스 쌍 | 그룹 수 | 비고 |
|-------------|--------|------|
| 오징어채볶음 ↔ 고추장진미채볶음 | 2그룹 | 시각적 유사성 높음 |
| 장조림 ↔ 메추리알장조림 | 2그룹 | 상위/하위 관계 |
| 갈비구이 ↔ 갈비찜 | 1그룹 | 동일 재료 다른 조리법 |
| 라볶이 ↔ 떡볶이 | 1그룹 | 시각적 유사성 높음 |
| 고추튀김 ↔ 꽈리고추무침 | 1그룹 | 동일 재료 다른 조리법 |
| 고추튀김 ↔ 오징어튀김 | 1그룹 | 튀김류 유사성 |

클래스 간 중복은 모델 입장에서 동일한 이미지에 서로 다른 정답이 부여된 모순 상황을 만들어 학습을 방해하므로 반드시 제거 또는 레이블 수정이 필요

---
