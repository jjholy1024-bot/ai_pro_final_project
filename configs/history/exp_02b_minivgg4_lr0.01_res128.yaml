experiment_name: "KFood_HighLR"

dataset:
  name: "KFood"
  data_dir: "./data/kfood_dataset"
  batch_size: 64
  num_workers: 4

model:
  name: "MyModel"
  in_channels: 3

train:
  epochs: 10
  learning_rate: 0.005
