experiment_name: "KFood_Ensemble_SE_224"

dataset:
  name: "KFood"
  data_dir: "./data/kfood_dataset"
  batch_size: 32
  num_workers: 4

model:
  name: "MyModel"
  in_channels: 3
  dropout: 0.5

train:
  epochs: 40
  learning_rate: 0.001
