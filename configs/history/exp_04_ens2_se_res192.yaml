experiment_name: "KFood_Ensemble_v2_192"

dataset:
  name: "KFood"
  data_dir: "./data/kfood_dataset"
  batch_size: 64
  num_workers: 4

model:
  name: "MyModel"
  in_channels: 3
  dropout: 0.5

train:
  epochs: 50
  learning_rate: 0.001
