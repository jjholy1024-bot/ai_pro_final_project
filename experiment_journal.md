# K-Food 이미지 분류 프로젝트: 실험 및 의사결정 저널 (Master Journal)

이 문서는 프로젝트의 시작부터 최종 모델 완성까지의 모든 실험 과정, 기술적 의사결정, 그리고 그 뒤에 담긴 논리적 근거를 기록합니다. (최종 보고서 작성용 자료)

---

## 1단계: 프로젝트 베이스라인 구축 (2026-06-02)

### [실험 01] Baseline 모델 수립
- **목적**: 데이터 파이프라인(Loader, Model, Train/Val)의 정상 작동 확인 및 기준 점수 확보.
- **설계**: 2층의 단순 CNN 구조 사용.
- **결과**: 검증 정확도 **19.84%**.
- **분석 및 직관**:
    - 150종의 방대한 음식 클래스를 구분하기에 모델의 깊이가 너무 얕음.
    - 데이터의 복잡도(배경, 조명 등)를 이겨낼 규제 장치(Dropout 등)가 부재함.
    - 10 Epoch는 충분한 학습을 보장하지 못함.

---

## 2단계: 모델 구조의 근본적 개선 및 실험 통제권 확보 (2026-06-03)

### [전략] 완성형 설계도 구축 및 코드 고정
- **의사결정 배경**: 교수님의 "코드 고정, YAML 실험" 원칙을 준수하기 위해, 본격적인 파라미터 최적화 전에 '기초 체력'이 검증된 모델 구조를 먼저 확립하기로 함.
- **기술적 논리**:
    - **MiniVGG 구조**: 층을 4개 블록으로 확장하여 고수준 특징 추출 능력 강화.
    - **Batch Normalization**: 학습 안정성 및 속도 향상.
    - **Dropout (0.5)**: 과적합 방지를 통한 일반화 성능 확보.
    - **Configurability**: YAML을 통해 Dropout rate 등을 조정할 수 있도록 코드 구조화.

### [실험 02] KFood_MiniVGG_BN_Dropout (완료)
- **설정 파일**: `configs/exp_02_minivgg_bn_dropout.yaml`
- **주요 변경**: Epoch 10 -> 30, MiniVGG 구조 적용, BN 및 Dropout 도입.
- **최종 성과**: **47.40%** 달성 (Baseline 19.84% 대비 **약 2.4배** 향상).
- **직관 확인**: 모델 구조를 수업 내용에 맞춰 보강하는 것만으로도 성능이 비약적으로 상승함을 입증함. 특히 BN이 학습 속도를 크게 높여주었으며, Dropout이 40% 이후의 완만한 상승 곡선을 지탱해줌.

---

## 3단계: 데이터 전처리 안정성 확보 및 향후 고도화 전략 수립 (2026-06-04)

### [작업 내용] 전처리 파이프라인(`transforms.py`) 최적화 및 안정화
- **의사결정 및 수정 사항**: 
  - `transforms.py`에 누락되어 있던 `T.Resize((128, 128))`를 명시적으로 추가하여, 규격 외 이미지가 입력되더라도 에러 없이 배치(Batch) 처리가 가능하도록 안전장치를 마련함.
  - 텐서 변환(`ToTensor()`) **이전**(PIL 이미지 상태)에 `Resize`를 배치함. 이는 텐서 변환 후 리사이즈를 하는 것보다 메모리 효율성과 연산 속도 측면에서 유리하다는 기술적 판단에 기반함. 이를 검증셋(Validation)에도 동일하게 적용하여 평가 시 모델 안정성을 보장함.

### [전략 기획] 향후 고도화를 위한 6대 핵심 아이디어 (Ideation)
가이드라인의 제약(외부 모델 사용 금지, `train.py` 등 핵심 실행 파일 수정 금지)을 준수하면서도 모델 성능을 극대화하기 위해 다음과 같은 창의적 전략을 수립함. (추후 타 환경에서 작업 재개 시 핵심 지침으로 활용)

**1. 데이터 전처리 및 증강 설계 (`utils/transforms.py`)**
- **크기 규격화 및 고해상도화**: 음식 이미지 특성상 미세한 입자, 고명 등의 디테일한 텍스처(Texture)가 분류의 핵심 단서임. 현재 128x128인 `T.Resize()` 해상도를 **224x224**로 상향 조정하여 정보 손실을 방지.
- **강력한 Data Augmentation**: 음식 카테고리는 앵글과 조명에 따라 과적합(Overfitting)이 매우 쉽게 발생함. 허용된 파일 내에서 `RandomHorizontalFlip()`, `RandomRotation(15)`, `ColorJitter()` 등의 강력한 증강 조합을 정의하고 적용하여 일반화 성능을 극대화.

**2. 모델 구조화 및 유연성 확보 (`models/my_model.py`)**
- **Adaptive Pooling 도입**: 분류기 직전에 `AdaptiveAvgPool2d`를 배치. 이를 통해 입력 해상도가 변경(예: 128 -> 224)되더라도 코드 수정 없이 유연하게 대응 가능한 아키텍처 구축.
- **과적합 제어 (공간적 Dropout)**: 합성곱 블록(Conv Block) 내부에도 마일드한 `Dropout2d` 레이어를 촘촘하게 배치. 특히 공간적 특징 정보가 응축되는 **뒤쪽 블록의 끝단에 약한 Dropout을 추가**하여 150종 음식 데이터셋에 대한 공간적 과적합을 한 번 더 강하게 억제함.

**3. 규제 우회형 '내부 앙상블' 아키텍처 설계 (`my_model.py`)**
- **직관 및 논리**: 템플릿 코드 및 가이드라인 제약상 외부 스크립트를 통한 명시적인 앙상블(Ensemble) 기법 구현은 불가함. 이를 우회하기 위해 `MyModel`의 `__init__` 내부에 **동일한 가중치 형식을 가진 독립된 서브 모델(Sub-model) 3개를 생성**하고, `forward` 단계에서 이들의 출력을 합산하는 **'내부 앙상블(멀티 컬럼)'** 구조를 직접 표방하여 단일 모델의 한계를 돌파.

**4. 실험 오케스트레이션 및 런타임 제어 (`configs/`)**
- 전이 학습이나 `train.py` 수정은 가이드라인 위반임. 
- 대신 여러 개의 YAML 설정 파일(LR, Batch Size 등 조합 변형)을 미리 생성해 두고, 이를 순차적으로 실행하는 **실험 오케스트레이션 파이프라인**을 구축. 이를 통해 최적의 학습률과 배치 사이즈 조합을 자동 탐색함.

---

## 3단계: 성능 극대화 전략 및 고도화 (2026-06-05)

### [실험 03] 내부 앙상블 및 SE Block 도입 모델
- **목적**: 단일 모델의 한계를 극복하기 위해 내부 앙상블 구조를 채택하고, 최신 어텐션 기법(SE Block)을 적용하여 성능을 극대화함.
- **주요 변경 사항**:
    1. **모델 구조**: 3개의 독립적인 CNN 컬럼을 가진 내부 앙상블(Internal Ensemble) 구현. 각 컬럼은 MiniVGG 스타일로 구성됨.
    2. **Squeeze-and-Excitation (SE) Block**: 채널 간의 상호 의존성을 학습하여 중요한 특징을 강조하도록 각 블록 끝에 배치.
    3. **Adaptive Pooling**: `AdaptiveAvgPool2d(1)`을 사용하여 입력 해상도에 유연하게 대응하고 특징을 압축함.
    4. **해상도 상향**: 기존 128에서 224로 해상도를 상향하여 이미지 정보 손실 최소화.
    5. **강력한 데이터 증강**: `ColorJitter`, `RandomAffine`, `RandomErasing`을 추가하여 과적합 방지 및 일반화 성능 향상.
- **가설**: 3개의 컬럼이 서로 다른 초기값에서 학습을 시작하여 다양한 특징을 포착하고, SE Block이 중요한 음식 특징(고명, 색상 등)을 강조함으로써 60% 이상의 검증 정확도를 달성할 것으로 기대함.
- **설정 파일**: `configs/exp_03_ensemble.yaml` (Epoch 40, Batch Size 32, LR 0.001)

---

---

## 4단계: 시행착오와 전략적 피벗 (Strategic Pivot) (2026-06-06)

### [시행착오 1] 과도한 복잡도 도입의 한계
- **현상**: 실험 03에서 3-Column 앙상블, SE Block, 224px 해상도를 한꺼번에 도입했으나 연산 부하로 학습 중단.

### [분석] Ablation Study (실험 05a/b/c) 결과
- **Fast_Base**: 10.51%
- **Fast_SE**: 13.36% (+2.85%p)
- **Fast_Ens**: 14.47% (+3.96%p)
- **결론**: 앙상블과 SE Block 모두 개별적으로 유효함을 확인. 이를 결합하되 구조적 군살을 제거하는 것이 핵심.

### [시행착오 2] 로컬 하드웨어 자원 한계 직면
- **문제**: 로컬 RTX 2060(6GB VRAM) 환경에서 고도화된 모델 학습 시 1 Epoch당 1시간 소요. 60% 이상의 고득점 달성을 위한 충분한 Epoch 확보 불가.
- **결정**: **Google Colab (T4 GPU)**으로 플랫폼 이주 결정. 16GB VRAM을 활용해 배치 사이즈를 128로 상향하고 연산 속도를 5배 이상 가속함.

---

## 5단계: 최종 'Golden Standard' 아키텍처 및 코랩 학습 (2026-06-06)

### [전략] 과적합 해결과 고효율 연산 설계
- **핵심 기술**:
    1. **GAP (Global Average Pooling)**: 파라미터 과다로 인한 '암기' 방지.
    2. **5-Block Depth**: 특징 추출 능력 강화.
    3. **Colab-AMP**: Mixed Precision 학습으로 속도 및 메모리 최적화.
- **의사결정 논리**: "하드웨어 한계를 피하지 않고 적합한 자원을 찾아 이동하는 것 또한 중요한 엔지니어링 의사결정"임을 명시.

#### [실험 06] KFood_Colab_Golden_v1 (진행 중)
- **설정**: 2-Column Ensemble, SE Block ON, GAP 기반 분류기, 192px, Batch 128, LR 0.0005.
- **아카이브**: 모든 변천사 코드는 `history/` 폴더에 버전별로 보존함.

---

## 향후 계획 (Planned)


## 5단계: 최종 'Golden Standard' 아키텍처 확립 (2026-06-06)

### [전략] 실험을 위한 최적의 실험실(Code) 완성
- **배경**: 단순 파라미터 튜닝만으로는 50%의 벽을 넘기 어렵다는 판단하에, 수업의 정석 기법을 총동원한 최종 구조를 확정함.
- **핵심 기술 및 도입 근거**:
    1. **Global Average Pooling (GAP)**: 기존의 무거운 Linear 층을 제거. 파라미터를 획기적으로 줄여 '이미지 암기(과적합)'를 원천 차단하고 '특징 이해' 위주로 학습하도록 강제함.
    2. **5-Block Deep CNN**: GAP로 확보된 연산 자원을 모델의 깊이에 재투자. 150종 분류를 위한 고수준 특징 추출 능력 극대화.
    3. **Hybrid Ensemble & SE**: 이전 Ablation Study에서 검증된 '2-Column 앙상블'과 'SE Block(채널 어텐션)'을 결합하여 최상의 시너지 유도.
    4. **Precision Training**: 학습 안정성을 위해 학습률을 **0.0005**로 하향 조정하고, 해상도 **192**를 고수하여 속도와 디테일의 균형을 맞춤.
- **의사결정 논리**: "이 구조를 최종 표준(Golden Standard)으로 고정하고, 이후의 성능 변화는 오직 YAML 설정을 통해서만 관찰한다"는 원칙을 수립.

#### [실험 06] KFood_Final_Golden_v1
- **설정**: 2-Column Ensemble, SE Block ON, 192px, LR 0.0005, 40 Epoch.
- **목표**: 토요일 18시 전까지 60% 이상의 검증 정확도 달성 및 최종 모델 확정.

---

---

## 6단계: 70% 고지 탈환을 위한 '이종 하이브리드' 승부수 (2026-06-06)

### [분석] 실험 06 (Colab Golden v1) 결과 분석
- **최종 성과**: **55.29%** (46 Epoch) 달성.
- **관찰**: 50%의 벽은 넘었으나, 비슷한 양념 기반 음식들(갈치조림, 고등어조림 등)의 구분에서 한계를 보임. 동일한 구조의 앙상블은 한 번 틀리기 시작하면 함께 틀리는 '편향성'이 존재함.

### [전략] '이종 앙상블(Heterogeneous Ensemble)' 도입
- **기술적 논리**: 서로 다른 시각적 깊이(Receptive Field)를 가진 모델들을 결합하여 다양성 확보.
    1. **Column 1 (5-Block)**: 전반적인 음식 패턴 파악 담당.
    2. **Column 2 (6-Block)**: 더 깊은 레이어를 통해 미세한 식재료 질감 파악 담당.
- **타임어택(Time-Attack) 설계**: 
    - 자정 전 결과를 도출하기 위해 배치 사이즈를 **192**로 상향하여 연산 속도 극대화.
    - 30 Epoch 안에 70% 도달을 목표로 학습률을 **0.0007**로 소폭 조정.

#### [실험 07] exp_07_hetero_ens5_6_res224_e50
- **목적**: 이종 앙상블과 강력한 규제 설계를 통한 70% 검증 정확도 돌파.
- **엔지니어링 의사결정 및 논리**:
    ... (중략) ...
    5. **Epoch 조정 (50 Epoch)**: 모델의 수렴 속도와 효율성을 고려하여 50 Epoch로 설정함.
    6. **Batch Size 조정 (64)**: 최초 128로 시도했으나, 224px 해상도와 이종 앙상블 구조의 결합으로 인한 VRAM 부족(OOM) 발생. 안정적인 학습을 위해 배치 사이즈를 64로 하향 조정함.
- **가설**: 이종 구조의 결합과 공간적 규제가 시너지를 내어, 기존 55%에서 정체되었던 성능을 70% 부근까지 끌어올릴 수 있을 것임.
- **설정 파일**: `configs/exp_07_hetero_ens5_6_res224_e50.yaml`

---

## 향후 계획 (Planned)
- 실험 07 완료 후 `evaluate.py`를 활용하여 혼동 행렬(Confusion Matrix) 분석.
- 특히 양념 기반 음식들 간의 오분류 사례를 정성적으로 분석하여 보고서에 반영.
