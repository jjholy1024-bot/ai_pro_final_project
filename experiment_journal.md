# K-Food 이미지 분류 프로젝트: 실험 및 의사결정 저널 (Master Journal)

이 문서는 프로젝트의 시작부터 최종 모델 완성까지의 모든 실험 과정, 기술적 의사결정, 그리고 그 뒤에 담긴 논리적 근거를 기록합니다. (최종 보고서 작성용 자료)

---

## 1단계: 프로젝트 베이스라인 구축 (2026-06-02)

### [실험 01] Baseline 모델 수립
- **목적**: 데이터 파이프라인(Loader, Model, Train/Val)의 정상 작동 확인 및 기준 점수 확보.
- **설계**: 2층의 단순 CNN 구조 사용.
- **결과**: 검증 정확도 **19.84%**.
- **분석 및 직관**:
    - 150종의 방대한 음식 클래스를 구분하기에 모델의 깊이가 너무 얕음.
    - 데이터의 복잡도(배경, 조명 등)를 이겨낼 규제 장치(Dropout 등)가 부재함.
    - 10 Epoch는 충분한 학습을 보장하지 못함.

---

## 2단계: 모델 구조의 근본적 개선 및 실험 통제권 확보 (2026-06-03)

### [전략] 완성형 설계도 구축 및 코드 고정
- **의사결정 배경**: 교수님의 "코드 고정, YAML 실험" 원칙을 준수하기 위해, 본격적인 파라미터 최적화 전에 '기초 체력'이 검증된 모델 구조를 먼저 확립하기로 함.
- **기술적 논리**:
    - **MiniVGG 구조**: 층을 4개 블록으로 확장하여 고수준 특징 추출 능력 강화.
    - **Batch Normalization**: 학습 안정성 및 속도 향상.
    - **Dropout (0.5)**: 과적합 방지를 통한 일반화 성능 확보.
    - **Configurability**: YAML을 통해 Dropout rate 등을 조정할 수 있도록 코드 구조화.

### [실험 02] KFood_MiniVGG_BN_Dropout (완료)
- **설정 파일**: `configs/exp_02_minivgg_bn_dropout.yaml`
- **주요 변경**: Epoch 10 -> 30, MiniVGG 구조 적용, BN 및 Dropout 도입.
- **최종 성과**: **47.40%** 달성 (Baseline 19.84% 대비 **약 2.4배** 향상).
- **직관 확인**: 모델 구조를 수업 내용에 맞춰 보강하는 것만으로도 성능이 비약적으로 상승함을 입증함. 특히 BN이 학습 속도를 크게 높여주었으며, Dropout이 40% 이후의 완만한 상승 곡선을 지탱해줌.

---

## 3단계: 데이터 전처리 안정성 확보 및 향후 고도화 전략 수립 (2026-06-04)

### [작업 내용] 전처리 파이프라인(`transforms.py`) 최적화 및 안정화
- **의사결정 및 수정 사항**: 
  - `transforms.py`에 누락되어 있던 `T.Resize((128, 128))`를 명시적으로 추가하여, 규격 외 이미지가 입력되더라도 에러 없이 배치(Batch) 처리가 가능하도록 안전장치를 마련함.
  - 텐서 변환(`ToTensor()`) **이전**(PIL 이미지 상태)에 `Resize`를 배치함. 이는 텐서 변환 후 리사이즈를 하는 것보다 메모리 효율성과 연산 속도 측면에서 유리하다는 기술적 판단에 기반함. 이를 검증셋(Validation)에도 동일하게 적용하여 평가 시 모델 안정성을 보장함.

### [전략 기획] 향후 고도화를 위한 6대 핵심 아이디어 (Ideation)
가이드라인의 제약(외부 모델 사용 금지, `train.py` 등 핵심 실행 파일 수정 금지)을 준수하면서도 모델 성능을 극대화하기 위해 다음과 같은 창의적 전략을 수립함. (추후 타 환경에서 작업 재개 시 핵심 지침으로 활용)

**1. 데이터 전처리 및 증강 설계 (`utils/transforms.py`)**
- **크기 규격화 및 고해상도화**: 음식 이미지 특성상 미세한 입자, 고명 등의 디테일한 텍스처(Texture)가 분류의 핵심 단서임. 현재 128x128인 `T.Resize()` 해상도를 **224x224**로 상향 조정하여 정보 손실을 방지.
- **강력한 Data Augmentation**: 음식 카테고리는 앵글과 조명에 따라 과적합(Overfitting)이 매우 쉽게 발생함. 허용된 파일 내에서 `RandomHorizontalFlip()`, `RandomRotation(15)`, `ColorJitter()` 등의 강력한 증강 조합을 정의하고 적용하여 일반화 성능을 극대화.

**2. 모델 구조화 및 유연성 확보 (`models/my_model.py`)**
- **Adaptive Pooling 도입**: 분류기 직전에 `AdaptiveAvgPool2d`를 배치. 이를 통해 입력 해상도가 변경(예: 128 -> 224)되더라도 코드 수정 없이 유연하게 대응 가능한 아키텍처 구축.
- **과적합 제어 (공간적 Dropout)**: 합성곱 블록(Conv Block) 내부에도 마일드한 `Dropout2d` 레이어를 촘촘하게 배치. 특히 공간적 특징 정보가 응축되는 **뒤쪽 블록의 끝단에 약한 Dropout을 추가**하여 150종 음식 데이터셋에 대한 공간적 과적합을 한 번 더 강하게 억제함.

**3. 규제 우회형 '내부 앙상블' 아키텍처 설계 (`my_model.py`)**
- **직관 및 논리**: 템플릿 코드 및 가이드라인 제약상 외부 스크립트를 통한 명시적인 앙상블(Ensemble) 기법 구현은 불가함. 이를 우회하기 위해 `MyModel`의 `__init__` 내부에 **동일한 가중치 형식을 가진 독립된 서브 모델(Sub-model) 3개를 생성**하고, `forward` 단계에서 이들의 출력을 합산하는 **'내부 앙상블(멀티 컬럼)'** 구조를 직접 표방하여 단일 모델의 한계를 돌파.

**4. 실험 오케스트레이션 및 런타임 제어 (`configs/`)**
- 전이 학습이나 `train.py` 수정은 가이드라인 위반임. 
- 대신 여러 개의 YAML 설정 파일(LR, Batch Size 등 조합 변형)을 미리 생성해 두고, 이를 순차적으로 실행하는 **실험 오케스트레이션 파이프라인**을 구축. 이를 통해 최적의 학습률과 배치 사이즈 조합을 자동 탐색함.

---

## 향후 계획 (Planned)
- 위 6대 기획 전략 중 가장 핵심적인 `models/my_model.py`의 구조 개편(Adaptive Pooling, 내부 앙상블 적용, Conv Block Dropout) 작업 착수.
- 입력 해상도를 224로 올리고, 강력한 증강을 포함하도록 `transforms.py` 업데이트.
- `configs/` 폴더에 오케스트레이션용 YAML 파일 세팅.
