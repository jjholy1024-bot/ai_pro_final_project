experiment_name: "KFood_Improved_v2"

dataset:
  name: "KFood"
  data_dir: "./data/kfood_dataset"
  batch_size: 64
  num_workers: 4

model:
  name: "MyModel"
  in_channels: 3
  dropout: 0.5

train:
  epochs: 30
  learning_rate: 0.001
