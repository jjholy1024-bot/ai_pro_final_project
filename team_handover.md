# 팀 프로젝트 중간 공유 (2026-06-02)

## 1. 현재 진행 상황
- **Phase 3(실행) 완료:** 베이스라인 모델 학습을 성공적으로 마쳤습니다.
- **주요 성과:** 10 에포크 학습 결과, 검증 정확도(Val Acc) **19.84%** 달성.
- **소스 관리:** 현재 모든 최신 코드는 `main` 브랜치에 푸시되어 있습니다.

## 2. 주요 변경 및 수정 사항
- **데이터 로더 수정 (`datasets/kfood.py`):**
    - `cv2` 의존성을 제거하고 `PIL` 기반으로 완전히 전환했습니다. (환경 구성 복잡도 감소)
    - 이미지 경로 매핑 로직을 최적화하여 데이터 로딩 속도를 개선했습니다.
- **모델 아키텍처 (`models/my_model.py`):**
    - 다양한 입력 해상도에 대응하기 위해 `AdaptiveAvgPool2d`를 추가하여 구조적 유연성을 확보했습니다.
- **설정 자동화 (`configs/`):**
    - `exp_01_baseline.yaml`을 통해 실험 환경을 규격화했습니다.

## 3. 학습 결과 확인 방법
- **상세 수치:** `runs/KFood_Baseline/training_summary.md` 파일에 에포크별 로그가 정리되어 있습니다.
- **체크포인트:** 가장 성능이 좋았던 모델은 `runs/KFood_Baseline/best_model.pth`에 저장되어 있습니다. (용량 문제로 git에서는 제외됨, 필요시 로컬에서 확인 가능)

## 4. 다음 단계 제안
- 현재 정확도가 꾸준히 우상향하고 있으므로, 에포크를 늘리거나 학습률(LR) 스케줄러를 적용하는 고도화 작업이 필요합니다.
- `utils/visualize.py`를 활용한 오답 분석(Error Analysis) 진행 예정입니다.
